services:

  mysql:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      # FIXME remove password from here
      MYSQL_ROOT_PASSWORD: Zxsw21qaZ!
    ports:
      - 3306:3306